
import {x} from "./x.wxs";

const foo = "'hello world' from comm.wxs" + Date.now();
const bar = function (d: string) {
    return decodeURI(d);
}

module.exports = {
    foo: foo,
    bar: bar,
    a: "{{APP_ID}}"
};